# -*- coding: utf-8 -*-
"""3주차(N3 Neural NLP Foundations + N3L Lab 2) hard 문제은행 생성기.
출력: work/nlp/bank/w3_hard.json
개념 두세 개 엮기, 비슷한 것 구별, 사례 적용, 왜 를 묻는 문항 중심.
계산 문항 정답은 아래에서 numpy/python 으로 실제 계산하고 assert 로 확인한다."""
import json, math, os
import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
OUT = os.path.join(HERE, "w3_hard.json")

U1 = "3-1 분류와 뉴런"
U2 = "3-2 비선형과 MLP"
U3 = "3-3 텍스트 CNN"
U4 = "3-4 미분과 연쇄 법칙"
U5 = "3-5 역전파와 계산 그래프"
U6 = "3-6 언어 모델과 n-gram"
U7 = "3-7 퍼플렉서티"
U8 = "3-8 신경망 LM 과 RNN"

bank = []
cnt = {}


def add(t, unit, slides, src=None, **kw):
    cnt[t] = cnt.get(t, 0) + 1
    item = {"id": f"w3h-{t}-{cnt[t]:03d}", "type": t, "part": "3", "level": "hard",
            "unit": unit, "slides": slides}
    if src:
        item["src"] = src
    item.update(kw)
    bank.append(item)


def mcq(unit, slides, q, c, a, e, src="예상"):
    add("mcq", unit, slides, src, q=q, c=c, a=a, e=e)


def ox(unit, slides, q, a, e, src="예상"):
    add("ox", unit, slides, src, q=q, a=a, e=e)


def short(unit, slides, q, a, e, src="예상", num=False, tol=None):
    kw = dict(q=q, a=a, e=e)
    if num:
        kw["num"] = True
        kw["tol"] = tol if tol is not None else 0.01
    add("short", unit, slides, src, **kw)


def essay(unit, slides, model, q, answer, points, src="예상"):
    add("essay", unit, slides, src, model=model, q=q, answer=answer, points=points)


def calc(unit, slides, model, q, qko, blanks, steps, answer, e, src="예상"):
    add("calc", unit, slides, src, model=model, q=q, qko=qko, blanks=blanks,
        steps=steps, answer=answer, e=e)


# =====================================================================
# MCQ
# =====================================================================
# ---- 3-1 분류와 뉴런
mcq(U1, "N3 p.7",
    "**교차 엔트로피(Cross-Entropy)**가 정답 확률의 음의 로그를 쓰는 까닭으로 가장 적절한 것은?",
    ["정답 확률이 작을수록 벌점이 급격히 커져 크게 틀린 예측을 강하게 벌주기 때문",
     "로그를 쓰면 확률의 합이 1이 되기 때문",
     "음수 점수를 양수로 바꿔 주기 때문",
     "확률을 0과 1 사이로 눌러 주기 때문"],
    0,
    "-log 0.9 ≈ 0.11 이지만 -log 0.01 ≈ 4.61 이에요. 정답에 낮은 확률을 준 경우에 벌점이 폭발적으로 커져요. 합이 1이 되게 하는 것과 0~1 로 누르는 것은 **소프트맥스(Softmax)**의 일이고, exp 가 값을 양수로 만들어요.")

mcq(U1, "N3 p.8, p.11",
    "**선형 분류기(Linear Classifier)**로 갈라지지 않는 XOR 배치를 신경망이 다룰 수 있게 되는 원리로 교수님이 설명한 것은?",
    ["데이터를 더 많이 모아 선을 여러 개 그린다",
     "**비선형성(Nonlinearity)**이 공간을 휘거나 차원을 올려서 한 경계면으로 가를 수 있게 만든다",
     "**학습률(Learning Rate)**을 아주 작게 준다",
     "**편향 항(Bias Term)**을 없앤다"],
    1,
    "교수님: 2차원에서 선으로 못 가르던 점들도 높이를 더해 한쪽은 높게 다른 쪽은 낮게 보내면 면 하나로 갈라져요. 선형층이 공간을 섞고 돌리면, **활성화 함수(Activation Function)**가 그 공간을 접거나 구부려요.")

mcq(U1, "N3 p.7, p.12",
    "**NER(개체명 인식)** 창 분류기를 **소프트맥스(Softmax)** 분류기에서 **MLP(다층 퍼셉트론)**로 바꿀 때 달라지는 점으로 가장 적절한 것은?",
    ["**손실 함수(Loss Function)**가 **교차 엔트로피(Cross-Entropy)**에서 다른 것으로 바뀐다",
     "입력이 **단어 벡터(Word Vector)**에서 글자로 바뀐다",
     "중간에 **은닉층(Hidden Layer)**과 **활성화 함수(Activation Function)**가 들어가 휜 **결정 경계(Decision Boundary)**를 그릴 수 있게 된다",
     "학습 방법이 **경사 하강법(Gradient Descent)**에서 세기(counting)로 바뀐다"],
    2,
    "입력(창 벡터)과 마지막 **소프트맥스(Softmax)**, **교차 엔트로피(Cross-Entropy)** **손실 함수(Loss Function)**, 그리고 **확률적 경사 하강법(Stochastic Gradient Descent (SGD))** 학습은 그대로예요. 달라지는 것은 가운데에 f(Wx + b) 층이 끼어 **비선형성(Nonlinearity)**이 생긴다는 점이에요.")

# ---- 3-2 비선형과 MLP
mcq(U2, "N3 p.10",
    "**시그모이드(Sigmoid)**의 약점과 **ReLU(렐루)**의 약점을 바르게 짝지은 것은?",
    ["시그모이드는 양 끝에서 기울기가 0에 가까워 **기울기 소실(Vanishing Gradient)**, ReLU 는 음수 쪽 기울기가 0이라 뉴런이 꺼진 채 남음",
     "시그모이드는 계산이 비싸서 느림, ReLU 는 출력이 0~1 로 제한됨",
     "시그모이드는 음수에서 꺼짐, ReLU 는 양 끝에서 기울기 소실",
     "둘 다 같은 이유로 **기울기 폭발(Exploding Gradient)**을 일으킴"],
    0,
    "교수님이 짚은 두 약점이에요. 시그모이드는 σ(4) ≈ 0.98 처럼 이미 평평해져 기울기가 거의 0이고, ReLU 는 음수 구간 기울기가 정확히 0 이라 dying ReLU 가 생겨요. 그래서 Leaky ReLU 와 **GELU(겔루)**가 나왔어요.")

mcq(U2, "N3 p.11",
    "선형층에 **편향 항(Bias Term)** b 가 있어도 **활성화 함수(Activation Function)**가 없으면 생기는 일로 옳은 것은?",
    ["b 덕분에 곡선 경계를 그릴 수 있다",
     "여전히 변환 하나(affine)로 합쳐져 깊이의 이득이 없다",
     "b 가 **비선형성(Nonlinearity)** 역할을 대신한다",
     "층마다 표현력이 곱셈으로 늘어난다"],
    1,
    "슬라이드: 'stacked linear layers collapse into one affine map'. b 는 경계를 평행 이동시킬 뿐 휘게 하지 못해요. 휘는 일은 오직 f 가 해요.")

mcq(U2, "N3 p.11",
    "**보편 근사 정리(Universal Approximation Theorem)**를 바르게 읽은 것은?",
    ["어떤 신경망이든 학습만 하면 모든 함수를 표현한다",
     "**은닉층(Hidden Layer)** 뉴런이 충분히 많으면 웬만한 함수를 근사할 수 있다는 존재 주장이다",
     "층이 깊을수록 반드시 성능이 좋아진다는 뜻이다",
     "선형층만으로도 모든 함수를 만들 수 있다는 뜻이다"],
    1,
    "슬라이드: 'enough hidden units can fit \"any\" reasonable function'. '뉴런이 충분히 많으면' 이라는 조건이 붙고, 실제로 그런 가중치를 학습으로 찾을 수 있다는 보장까지 주는 말은 아니에요.")

# ---- 3-3 텍스트 CNN
mcq(U3, "N3 p.14",
    "**최대 풀링(Max-pooling)**의 위치 무관 성질을 평가한 것으로 가장 적절한 것은?",
    ["감정, 주제 **분류(Classification)**에는 장점이지만 위치나 순서가 중요한 과제에서는 정보를 잃는 단점이다",
     "어떤 과제에서든 장점이기만 하다",
     "어떤 과제에서든 단점이기만 하다",
     "필터 폭을 늘리면 사라지는 성질이다"],
    0,
    "'어디에서든 나왔나?' 만 묻기 때문에 문장 어디에 있든 같은 결과가 나와요. 주제나 감정을 고를 때는 도움이 되지만, 먼 거리 순서가 필요한 과제에서는 **RNN(순환 신경망)**의 recurrence 가 필요해요.")

mcq(U3, "N3 p.13-14, p.37",
    "텍스트 **CNN(합성곱 신경망)**의 필터 뱅크와 n-gram **언어 모델(LM)**을 비교한 설명으로 가장 적절한 것은?",
    ["둘 다 연속된 몇 단어를 보지만, CNN 은 **임베딩(Embedding)** 위에서 학습된 패턴을 찾고 n-gram 은 표면 문자열 횟수를 센다",
     "둘 다 말뭉치에서 횟수를 세어 확률을 구한다",
     "둘 다 문장 전체의 순서를 본다",
     "CNN 은 횟수를 세고 n-gram 은 필터를 학습한다"],
    0,
    "폭 2~5 필터 묶음은 'a bank of learned n-gram detectors' 예요. 다만 n-gram 은 'opened their' 와 'opened her' 를 남남으로 세지만, CNN 은 **단어 벡터(Word Vector)** 위에서 계산해 비슷한 단어가 비슷하게 반응해요.")

# ---- 3-4 미분과 연쇄 법칙
mcq(U4, "N3 p.19, p.35",
    "3주차에 나오는 두 '연쇄 법칙' 을 바르게 구별한 것은?",
    ["둘 다 확률을 곱으로 쪼개는 규칙이다",
     "2부의 **연쇄 법칙(Chain Rule)**은 합성 함수 미분을 곱으로 쪼개고, 3부의 **확률의 연쇄 법칙(Chain Rule of Probability)**은 글 전체 확률을 조건부 확률의 곱으로 쪼갠다",
     "둘 다 미분을 곱으로 쪼개는 규칙이다",
     "2부는 확률, 3부는 미분에 대한 규칙이다"],
    1,
    "이름만 같고 대상이 달라요. 2부는 ∂h/∂x = (∂h/∂z)(∂z/∂x), 3부는 P(x1...xT) = Π P(x_t | x_(<t)) 예요. 손실 J 와 **야코비안(Jacobian)** J 도 글자만 같은 다른 것이에요.")

mcq(U4, "N3 p.21",
    "uᵀ diag(f'(z)) 를 **원소별 곱(Hadamard Product)**으로 바꿔 쓸 수 있는 까닭으로 옳은 것은?",
    ["u 가 늘 **단위 행렬(Identity Matrix)**이기 때문",
     "**대각 행렬(Diagonal Matrix)**은 대각선 밖이 모두 0이라 같은 자리 원소끼리만 곱해지기 때문",
     "**전치(Transpose)** 때문에 순서가 바뀌기 때문",
     "f 가 선형이기 때문"],
    1,
    "z = [3, -2], f = **ReLU(렐루)**, u = [1, 5] 라면 f'(z) = [1, 0] 이고 uᵀ diag(f'(z)) = [1×1, 5×0] = [1, 0] 이에요. 교수님도 동그라미 기호는 하다마드 곱, 곧 원소별 곱이라고 했어요.")

mcq(U4, "N3 p.18-19",
    "다음 짝 가운데 옳지 않은 것은?",
    ["입력 1개, 출력 1개 → 미분은 숫자 하나",
     "입력 n개, 출력 1개 → **기울기(Gradient)**는 길이 n 벡터",
     "입력 n개, 출력 m개 → **야코비안(Jacobian)**은 n×m 행렬",
     "신경망 층(벡터 → 벡터)의 미분 → **야코비안(Jacobian)**"],
    2,
    "**야코비안(Jacobian)**은 m×n 이에요. 행이 출력 수 m, 열이 입력 수 n 이에요. '출력이 먼저(행), 입력이 나중(열)' 으로 외워요.")

# ---- 3-5 역전파와 계산 그래프
mcq(U5, "N3 p.25-26",
    "x 가 5칸, W 가 (3, 5), δ 가 3칸일 때 ∂s/∂W 를 x δᵀ 로 잘못 쓰면 어떤 일이 생기는가?",
    ["(5, 3) 이 되어 W 와 shape 이 달라 업데이트를 할 수 없다",
     "(3, 5) 가 나와 문제가 없다",
     "곱셈 자체가 불가능해 오류가 난다",
     "값은 맞지만 부호가 뒤집힌다"],
    0,
    "(5×1)(1×3) = (5×3) 이라 W 의 (3, 5) 와 달라요. 파라미터의 기울기는 파라미터와 같은 shape 이어야 W ← W - α∂J/∂W 를 할 수 있어요. 코드를 돌리기 전에 shape 만 적어 봐도 잡히는 실수예요.")

mcq(U5, "N3 p.28, p.31",
    "학습이 추론보다 메모리를 훨씬 많이 쓰는 까닭으로 교수님이 든 설명은?",
    ["파라미터를 두 벌 저장해야 해서",
     "**순전파(Forward Pass)**의 중간값과 파라미터마다의 **기울기(Gradient)**까지 함께 들고 있어야 해서",
     "**학습률(Learning Rate)**을 저장해야 해서",
     "**계산 그래프(Computation Graph)**를 그림 파일로 저장해서"],
    1,
    "교수님: 파라미터가 100MB 인 모델을 학습하려면 기울기까지 담을 최소 2배 메모리가 필요하고, GPU 16GB 에 모델 10GB 면 추론은 되지만 학습은 안 된다고 했어요. 역방향 패스에서 **ReLU(렐루)**의 부호나 × 노드의 상대값 같은 순전파 값을 다시 써요.")

mcq(U5, "N3 p.30",
    "순전파에서 a = 1, b = 3, c = 2 이고 f = (a + b) × max(b, c) 일 때 역전파로 얻는 ∂f/∂b 는?",
    ["3", "4", "7", "0"],
    2,
    "× 노드는 상대 값을 곱해요. + 쪽으로 max(b, c) = 3, max 쪽으로 (a + b) = 4 가 흘러요. + 노드는 그대로 나눠 주어 b 에 3, max 노드는 이긴 b 에 4 를 줘요. b 가 두 곳에 쓰였으니 더해서 3 + 4 = 7 이에요.")

# ---- 3-6 언어 모델과 n-gram
mcq(U6, "N3 p.39",
    "n-gram 의 '유사도 없음' 문제가 2주차의 어떤 문제와 같은 뿌리인지 고르면?",
    ["**지프의 법칙(Zipf's Law)**",
     "**원-핫 벡터(One-hot Vector)**끼리 내적이 0이라 비슷함을 전혀 모르는 문제",
     "**토큰 과다 분할(Fertility)**",
     "**병합 규칙(Merge Rule)**의 순서 문제"],
    1,
    "슬라이드: 'no notion of similarity (week 2's disease, again)'. 'opened their' 와 'opened her' 가 횟수를 전혀 나누지 못해요. 해결은 단어를 좌표로 놓는 **임베딩(Embedding)**이에요.")

mcq(U6, "N3 p.39",
    "n-gram 에서 n 을 키울 때의 트레이드오프로 가장 적절한 것은?",
    ["문맥이 길어지는 대신 **희소성 문제(Sparsity Problem)**와 저장 부담이 커진다",
     "문맥도 길어지고 희소성도 줄어든다",
     "문맥은 그대로이고 저장만 늘어난다",
     "문맥이 짧아지고 저장도 줄어든다"],
    0,
    "**어휘(Vocabulary)**가 1만이면 2단어 묶음 조합이 1억, 3단어면 1조 가지라 대부분 0번이 돼요. 슬라이드: 'it gets WORSE as n grows, in practice n ≤ 5'.")

mcq(U6, "N3 p.40",
    "n-gram 생성 예에서 'today the price of gold is shiny' 가 나온 까닭을 바르게 설명한 것은?",
    ["'gold is shiny' 라는 세 단어 묶음 자체는 자연스럽지만, 앞의 price 라는 문맥을 볼 수 없어서 전체가 어색해진 것",
     "모델이 계산을 잘못해서",
     "shiny 가 **어휘(Vocabulary)**에 없어서",
     "**샘플링(Sampling)**에서 늘 확률이 가장 낮은 후보를 고르기 때문"],
    0,
    "교수님: 로컬 플루언시는 챙기지만 롱 레인지 코히어런스는 챙길 수 없어요. trigram 은 앞 2단어만 보니 price 가 이미 기억에서 사라졌어요. 이 예에서는 우연히 매번 1등 후보가 뽑혔어요.")

# ---- 3-7 퍼플렉서티
mcq(U7, "N3 p.42, N3L p.25",
    "글자 단위 모델의 **퍼플렉서티(PPL)** 27.48 과 **서브워드(Subword)** 단위 모델의 PPL 299.07 을 그대로 비교하면 안 되는 까닭으로 가장 적절한 것은?",
    ["학습 시간이 달라서",
     "PPL 이 '예측 단위 하나당' 값이라 글자 하나를 맞히는 일과 덩어리 하나를 맞히는 일이 애초에 다른 자이기 때문",
     "한쪽이 더 큰 모델이라서",
     "PPL 은 원래 비교에 쓰는 값이 아니기 때문"],
    1,
    "실습에서 두 값을 bits per character 로 바꾸면 4.78 대 4.30 으로 오히려 **서브워드(Subword)** 모델이 더 좋았어요. 슬라이드도 같은 **어휘(Vocabulary)**와 같은 **토크나이저(Tokenizer)**일 때만 비교할 수 있다고 못 박아요.")

mcq(U7, "N3 p.41, N3L p.12",
    "실습 학습 로그에서 loss 3.254 일 때 함께 찍힌 ppl 25.9 는 어떻게 나온 값인가?",
    ["3.254 × 8 을 한 값",
     "exp(3.254) 를 한 값",
     "log(3.254) 를 한 값",
     "1 / 3.254 를 한 값"],
    1,
    "**퍼플렉서티(PPL)** = exp(평균 **교차 엔트로피(Cross-Entropy)**) 예요. e^3.254 ≈ 25.9 이고, 학습 코드도 `math.exp(loss.item())` 으로 찍어요. 학습이 줄이는 손실이 곧 PPL 을 내리는 손실이에요.")

mcq(U7, "N3 p.41-42",
    "어떤 모델의 **퍼플렉서티(PPL)**가 **어휘(Vocabulary)** 크기 V 와 거의 같게 나왔다면 어떻게 읽어야 하는가?",
    ["완벽에 가까운 모델이다",
     "사실상 아무것도 모르고 모든 후보에 고르게 확률을 나눠 준 수준이다",
     "**토크나이저(Tokenizer)**가 잘못된 것이다",
     "PPL 을 잘못 계산한 것이다"],
    1,
    "균등 분포면 PPL = V 가 되니 학습 전 수준이라는 뜻이에요. 실습에서도 글자 **어휘(Vocabulary)** 1945 에 대해 학습 뒤 PPL 25.92 가 나와 '학습 전이라면 1945 근처' 라고 비교해요.")

# ---- 3-8 신경망 LM 과 RNN
mcq(U8, "N3 p.48",
    "고정 윈도우 신경망 **언어 모델(LM)**과 **RNN(순환 신경망)** **언어 모델(LM)**을 비교한 표의 내용으로 옳지 않은 것은?",
    ["문맥 길이: 고정 윈도우는 고정, RNN 은 원리상 제한 없음",
     "가중치: 고정 윈도우는 자리마다 W 의 다른 부분, RNN 은 하나의 W_h 와 W_e 를 공유",
     "계산: 고정 윈도우는 한 번에 병렬, RNN 은 t 가 t-1 을 기다리는 순차 계산",
     "모델 크기: 고정 윈도우는 문장 길이와 무관, RNN 은 문장이 길수록 커짐"],
    3,
    "거꾸로예요. 표의 'model size' 칸은 고정 윈도우가 'grows with the window', RNN 이 'independent of sequence length' 예요. 두 모델 모두 실제로는 먼 과거를 잊는데, 고정 윈도우는 설계상, RNN 은 기울기가 흐려져서 잊어요.")

mcq(U8, "N3 p.50",
    "**시간 역전파(BPTT)**에서 W_h 의 **기울기(Gradient)**를 모든 시점에서 더하는 근거가 되는 2부의 규칙은?",
    ["max 노드는 이긴 쪽으로만 보낸다",
     "순전파에서 여러 곳에 쓰인 값은 역전파에서 기울기를 더한다",
     "곱셈 노드는 상대 값을 곱한다",
     "**파라미터(Parameter)**의 기울기는 파라미터와 같은 shape 이다"],
    1,
    "슬라이드: '(exactly the \"gradients sum at branches\" rule from part 2, nothing new was invented)'. W_h 가 모든 시점에 똑같이 쓰이므로 갈림길의 합 규칙이 그대로 적용돼요.")

mcq(U8, "N3 p.52-54",
    "**기울기 소실(Vanishing Gradient)**과 **기울기 폭발(Exploding Gradient)**의 원인과 대처를 바르게 짝지은 것은?",
    ["둘 다 **기울기 클리핑(Gradient Clipping)**으로 해결된다",
     "둘 다 시점마다의 **야코비안(Jacobian)** 곱에서 생기며, 폭발은 클리핑으로, 소실은 **게이트(Gate)**나 **어텐션(Attention)** 같은 구조로 다룬다",
     "소실은 **학습률(Learning Rate)**을 키우면 해결된다",
     "폭발은 구조를 바꿔야 하고 소실은 클리핑으로 해결된다"],
    1,
    "∂h^(t)/∂h^(1) 은 시점마다의 **야코비안(Jacobian)**을 곱한 것이라, 작은 값이 이어지면 0으로 사라지고 1보다 조금 큰 값이 이어지면 폭발해요. 슬라이드 결론: 'Clipping fixes exploding; vanishing needs a better ARCHITECTURE'.")

# =====================================================================
# OX
# =====================================================================
# ---- 3-1 분류와 뉴런
ox(U1, "N3 p.7",
   "**소프트맥스(Softmax)** 분류기를 그대로 두고 데이터를 아주 많이 모으면 'not bad' 같은 상호작용도 잡을 수 있다.",
   False,
   "자료가 아무리 많아도 **선형 분류기(Linear Classifier)**의 **결정 경계(Decision Boundary)**는 곧은 **초평면(Hyperplane)**이에요. 상호작용을 잡으려면 **비선형성(Nonlinearity)**을 가진 층이 필요해요.")

ox(U1, "N3 p.9",
   "**뉴런(Neuron)**의 **가중치(Weight)** w 를 여러 개 모아 행으로 쌓은 것이 **은닉층(Hidden Layer)**의 **행렬(Matrix)** W 이다.",
   True,
   "교수님: 여러 뉴런의 w 를 모아 놓은 것이 큰 행렬 W 예요. 그래서 W 의 행 수가 뉴런(출력) 수, 열 수가 입력 길이가 돼요.")

# ---- 3-2 비선형과 MLP
ox(U2, "N3 p.10",
   "**하이퍼볼릭 탄젠트(tanh)**는 출력 범위가 -1 에서 1 이라 **시그모이드(Sigmoid)**보다 **기울기 소실(Vanishing Gradient)** 문제에서 완전히 자유롭다.",
   False,
   "tanh 도 양 끝이 평평해져 기울기가 거의 0이 돼요. 출력 범위만 다를 뿐 같은 S자 모양이라 깊이 쌓으면 같은 문제가 생겨요. 그래서 안쪽 층 기본은 **ReLU(렐루)**나 **GELU(겔루)**예요.")

ox(U2, "N3 p.12, N3L p.3",
   "**은닉층(Hidden Layer)** 뉴런이 3개이고 입력이 5칸이면 W 의 shape 은 (5, 3) 이다.",
   False,
   "(3, 5) 예요. z = Wx 에서 (3×5)(5×1) = (3×1) 이 되어야 하니 행이 출력 뉴런 수, 열이 입력 수예요. 실습 N3L p.3 의 출력도 W : (3, 5) 로 찍혀요.")

# ---- 3-3 텍스트 CNN
ox(U3, "N3 p.13",
   "폭 3 **합성곱 필터(Convolution Filter)**를 6단어 문장에 한 칸씩 미끄러뜨리면 **특징 맵(Feature Map)**의 길이는 4 이다.",
   True,
   "창 개수 = 단어 수 - 폭 + 1 = 6 - 3 + 1 = 4 예요. 슬라이드 그림도 창 네 개에 값 네 개가 나와요.")

ox(U3, "N3 p.14",
   "여러 폭(2에서 5)의 필터를 여럿 두고 각각 **최대 풀링(Max-pooling)**하면, 필터 개수만큼의 숫자가 모여 그 위 분류기의 입력이 된다.",
   True,
   "필터 하나가 풀링 뒤에 숫자 하나를 남기니, 필터 개수가 곧 분류기 입력 벡터의 길이가 돼요. 이 묶음이 'a bank of learned n-gram detectors' 예요.")

# ---- 3-4 미분과 연쇄 법칙
ox(U4, "N3 p.17, p.19",
   "**손실 함수(Loss Function)** J(θ) 의 J 와 **야코비안(Jacobian)** J 는 같은 대상을 가리킨다.",
   False,
   "글자만 같고 다른 것이에요. J(θ) 는 벌점(손실)이고, **야코비안(Jacobian)** J 는 편미분을 모은 m×n **행렬(Matrix)**이에요. 문맥으로 구별해야 해요.")

ox(U4, "N3 p.21",
   "교수님은 야코비안 네 가지(W, I, diag(f'(z)), uᵀ)를 반드시 암기해야 시험을 볼 수 있다고 했다.",
   False,
   "교수님: 암기할 필요는 없지만 수식을 이해하려면 암기하면 좋다고 했어요. PyTorch 가 계산해 주지만, 식을 읽으려면 네 블록을 알아 두는 편이 좋아요.")

# ---- 3-5 역전파와 계산 그래프
ox(U5, "N3 p.23-24",
   "∂s/∂W 에는 x 가 붙고 ∂s/∂x 에는 W 가 붙는다.",
   True,
   "z = Wx + b 를 W 로 미분하면 x 쪽, x 로 미분하면 W 가 나와요. 곱하기 노드가 상대 값을 곱해 보내는 성질(p.30)과 같은 이야기예요.")

ox(U5, "N3 p.27-28",
   "**순전파(Forward Pass)**에서 저장해 둔 중간값은 역방향 패스에서 쓰이지 않으므로 바로 버려도 된다.",
   False,
   "**ReLU(렐루)**의 미분은 z 의 부호를 알아야 하고 × 노드는 상대 입력값이 필요해요. 그래서 중간값을 저장해 두었다가 **역방향 패스(Backward Pass)**에서 다시 써요.")

ox(U5, "N3 p.32, N3L p.6",
   "실습에서 손으로 구한 dW, db, dx 가 autograd 의 W.grad, b.grad, x.grad 와 일치했다는 것은, 우리가 유도한 식이 PyTorch 가 하는 계산과 같다는 뜻이다.",
   True,
   "N3L p.6 은 `torch.allclose` 로 셋을 모두 비교하고 'your calculus matches PyTorch exactly' 를 찍어요. 과제 2 Part A 가 이 작업을 **word2vec**에 대해 하라고 해요.")

# ---- 3-6 언어 모델과 n-gram
ox(U6, "N3 p.38",
   "슬라이드의 students opened their 예에서 n-gram 모델이 exams 대신 books 를 고른 것은 계산이 틀렸기 때문이다.",
   False,
   "슬라이드: 'The model is not wrong, it was FORCED to forget the words that mattered'. **마르코프 가정(Markov Assumption)** 때문에 proctor, clock 이라는 단서를 아예 볼 수 없었을 뿐이에요.")

ox(U6, "N3 p.35, p.40",
   "다음 단어의 **확률 분포(Probability Distribution)**를 낼 수 있으면 그 모델로 글을 생성할 수도 있다.",
   True,
   "분포에서 **샘플링(Sampling)**해 붙이고 다시 예측하기를 되풀이하면 돼요. 슬라이드: 'Same sampling loop as ChatGPT, the model quality is the only difference'.")

# ---- 3-7 퍼플렉서티
ox(U7, "N3 p.41",
   "**퍼플렉서티(PPL)**를 내리려고 **손실 함수(Loss Function)**와 다른 목표를 따로 두고 학습해야 한다.",
   False,
   "PPL = exp(J) 라서 학습이 줄이는 바로 그 **교차 엔트로피(Cross-Entropy)** J 가 내려가면 PPL 도 함께 내려가요. 슬라이드: 'it drops out of the SAME cross-entropy loss we already train with'.")

ox(U7, "N3L p.19, p.25",
   "실습에서 같은 727,466 글자 말뭉치가 **서브워드(Subword)** 토큰 390,136 개가 되었다는 것은 예측 한 번이 평균 약 1.9 글자를 담당하게 되었다는 뜻이다.",
   True,
   "727,466 ÷ 390,136 ≈ 1.9 예요. 예측 한 번의 단위가 커진 것이라, 같은 자로 재려면 bits per character 로 바꿔야 해요.")

# ---- 3-8 신경망 LM 과 RNN
ox(U8, "N3 p.45",
   "고정 윈도우 신경망 **언어 모델(LM)**에서는 같은 단어가 창의 1번 자리에 올 때와 3번 자리에 올 때 서로 다른 **가중치(Weight)**로 처리된다.",
   True,
   "슬라이드: 'each position has its OWN weights in W, \"the\" in slot 1 and slot 3 are learned separately'. 이 낭비를 없애려고 **RNN(순환 신경망)**의 가중치 공유로 갑니다.")

ox(U8, "N3 p.49",
   "**RNN(순환 신경망)** **언어 모델(LM)** 학습에서는 매 시점 모델이 예측한 단어를 다음 입력으로 넣는다.",
   False,
   "학습 때는 늘 진짜 앞부분을 조건으로 줘요(teacher forcing). 모델이 뽑은 것을 다시 넣는 것은 생성할 때의 고리예요.")

# =====================================================================
# SHORT
# =====================================================================
short(U1, "N3 p.7",
      "**소프트맥스(Softmax)** 분류기가 아직 '선형 기계' 인 까닭을 한 문장으로 쓰시오.",
      ["W 행렬 하나만 곱하기 때문", "가중치 행렬 하나만 곱해서", "선형 변환이 하나뿐이라서",
       "은닉층과 활성화 함수가 없어서", "비선형성이 없어서"],
      "슬라이드: 'So far this is just logistic regression, a LINEAR machine'. 교수님도 W 가 단 하나의 **행렬(Matrix)**로 표현된다는 점이 중요하다고 했어요. **소프트맥스(Softmax)** 자체는 확률로 바꿔 줄 뿐 **결정 경계(Decision Boundary)**를 휘게 하지 못해요.")

short(U2, "N3 p.11",
      "**활성화 함수(Activation Function)**가 층 사이에서 하는 일을 공간의 모양으로 설명할 때, 선형층이 '섞고 돌리고 늘린다' 면 활성화는 무엇을 하는지 한 낱말로 쓰시오.",
      ["휜다", "구부린다", "접는다", "휘게 한다", "구부리기", "접기"],
      "교수님: 선형층은 공간을 섞고 회전하고 늘리고 줄이고, 비선형은 그 공간을 접거나 구부려요. 슬라이드도 'After nonlinearity: grid is bent' 라고 적혀 있어요.")

short(U3, "N3 p.14",
      "**CNN(합성곱 신경망)**과 **RNN(순환 신경망)** 가운데, 먼 거리 순서가 중요한 과제에 더 알맞은 쪽을 쓰고 그 까닭을 한 구절로 덧붙이시오.",
      ["RNN, 기억을 가지고 순서대로 읽어서", "RNN", "순환 신경망", "RNN 이 은닉 상태로 기억을 이어가서"],
      "**CNN(합성곱 신경망)**은 창 안(로컬)만 봐요. 순서와 먼 거리 관계가 필요하면 **은닉 상태(Hidden State)**로 지금까지 읽은 것을 이어 가는 **RNN(순환 신경망)**이 맞아요.")

short(U4, "N3 p.20-21",
      "역전파에서 한 경로 위의 단계들은 곱하고, 여러 경로가 만나는 곳에서는 어떻게 하는지 한 낱말로 쓰시오.",
      ["더한다", "합한다", "덧셈", "합", "더하기"],
      "슬라이드 p.30: 'branches that MERGE forward will SUM their gradients backward'. 이 규칙이 뒤에서 **시간 역전파(BPTT)**의 합으로 이어져요.")

short(U5, "N3 p.31",
      "**파라미터(Parameter)**가 100만 개일 때, 기울기를 하나씩 흔들어 구하면 **순전파(Forward Pass)**가 몇 번 필요한지 숫자로 쓰시오.",
      ["1000000", "1,000,000"],
      "파라미터마다 따로 흔들어야 하니 100만 번이에요. **역전파(Backpropagation)**는 순방향 한 번 + 역방향 한 번이면 끝나요. 슬라이드: 'ALL gradients for the price of one'.",
      num=True)

short(U5, "N3 p.29",
      "**계산 그래프(Computation Graph)**의 한 노드가 전체 신경망을 몰라도 계산할 수 있는 성질을 무엇이라 하는지 한 낱말로 쓰시오.",
      ["locality", "로컬리티", "국소성", "지역성"],
      "슬라이드: 'This locality is why autodiff frameworks can be built at all'. 노드는 자기 **국소 기울기(Local Gradient)**만 알면 되고, 그 덕분에 PyTorch 같은 **자동 미분(Automatic Differentiation (Autodiff))** 도구를 만들 수 있어요.")

short(U6, "N3 p.39",
      "슬라이드가 든 n-gram 의 세 벽을 잇는 네 번째 한계, 곧 n-1 단어 밖은 아무리 중요해도 볼 수 없다는 문제를 부르는 말을 쓰시오.",
      ["버려진 문맥", "discarded context", "문맥 버림", "버려진 컨텍스트"],
      "슬라이드: 'Discarded context: everything beyond n-1 words is invisible, however important'. students opened their 예제의 proctor 와 clock 이 그 예예요.")

short(U7, "N3L p.25-26",
      "**토크나이저(Tokenizer)**가 서로 다른 두 **언어 모델(LM)**을 같은 자로 재려고 실습에서 쓴 지표의 이름을 쓰시오.",
      ["bits per character", "bits/char", "글자당 비트", "비트 퍼 캐릭터", "bpc"],
      "글자 모델 4.78, **서브워드(Subword)** 모델 4.30 이었어요. '이 모델이 한국어 글자 하나를 적는 데 몇 비트가 필요한가' 라는 같은 질문에 답하게 만들어 줘요.")

short(U7, "N3 p.42",
      "정답 확률이 네 번 모두 1/4 인 **언어 모델(LM)**의 **퍼플렉서티(PPL)**를 숫자로 쓰시오.",
      ["4"],
      "역수가 모두 4 이므로 (4 × 4 × 4 × 4)^(1/4) = 4 예요. 모든 자리의 확률이 같으면 PPL 은 그 확률의 역수와 같아요.",
      num=True)

short(U8, "N3 p.54",
      "**기울기 소실(Vanishing Gradient)**을 구조로 푸는 방법으로 슬라이드가 든, 무엇을 남기고 쓰고 내보낼지 학습으로 정하는 장치의 이름을 쓰시오.",
      ["게이트", "gate", "게이트(LSTM/GRU)", "LSTM 게이트"],
      "**장단기 메모리(LSTM)**와 GRU 는 셀 상태라는 고속도로에 **게이트(Gate)**를 얹어요. **기울기(Gradient)**가 그 길을 타고 흘러 먼 의존 관계를 배울 수 있게 돼요. 다음 주의 더 큰 해법은 **어텐션(Attention)**이에요.")

# =====================================================================
# ESSAY
# =====================================================================
essay(U1, "N3 p.6-7, p.12", "softmax-ce-pipeline",
      "창 기반 **NER(개체명 인식)** 분류기가 입력에서 벌점까지 가는 과정을 순서대로 서술하고, 이 분류기가 왜 아직 선형 모델인지 설명하시오.",
      "먼저 판단할 **중심 단어(Center Word)**와 양옆 **윈도우(Window)** 안 단어들의 **단어 벡터(Word Vector)**를 꺼내 이어 붙여 길이 5d 의 창 벡터 x 를 만든다. 다음으로 W 를 곱해 클래스마다 **로짓(Logit)**을 얻고, **소프트맥스(Softmax)**로 합이 1인 확률로 바꾼다. 정답 클래스에 준 확률의 음의 로그가 그 예의 벌점이고, 여러 예의 평균이 **교차 엔트로피(Cross-Entropy)** **손실 함수(Loss Function)** J(θ) 이다. 이 벌점을 줄이는 쪽으로 W 를 고치는 것이 학습이다. 이때 계산에 쓰이는 변환이 W 를 한 번 곱하는 것뿐이므로 **결정 경계(Decision Boundary)**가 곧은 **초평면(Hyperplane)**에 머물고, 사실상 **로지스틱 회귀(Logistic Regression)**와 같다. 가운데에 f(Wx + b) 층을 넣어 **MLP(다층 퍼셉트론)**로 만들면 비로소 휜 경계를 그릴 수 있다.",
      ["창 안 단어 벡터를 이어 붙여 x 만들기",
       "W 를 곱해 클래스별 로짓",
       "소프트맥스로 확률로 변환",
       "정답 확률의 -log 가 벌점",
       "평균이 교차 엔트로피 손실 J",
       "선형인 이유: 행렬 곱 하나뿐, 경계가 초평면",
       "로지스틱 회귀와 같음",
       "해결: 은닉층과 활성화 함수를 넣어 MLP 로"])

essay(U2, "N3 p.10", "activation-choice",
      "**시그모이드(Sigmoid)**, **ReLU(렐루)**, **GELU(겔루)**를 언제 쓰는지 각각의 성질과 약점을 들어 비교 서술하시오.",
      "**시그모이드(Sigmoid)**는 출력이 0에서 1 사이라 확률을 내거나 **장단기 메모리(LSTM)**의 **게이트(Gate)**처럼 얼마나 열지 정할 때 쓴다. 다만 양 끝이 평평해 기울기가 거의 0이 되므로 깊이 쌓으면 **기울기 소실(Vanishing Gradient)**이 생긴다. **ReLU(렐루)**는 max(z, 0) 으로 계산이 싸고 양수 쪽 기울기가 늘 1이라 학습이 빨라 안쪽 층의 기본 선택이다. 대신 음수 구간 기울기가 0이라 **뉴런(Neuron)**이 꺼진 채 남는 dying ReLU 가 생길 수 있고, 그래서 Leaky ReLU 가 나왔다. **GELU(겔루)**는 0 근처가 부드러운 ReLU 로 **트랜스포머(Transformer)**가 쓴다. 정리하면 안쪽 층에는 ReLU 나 GELU 를 쓰고, 확률이나 게이트가 필요할 때만 시그모이드를 쓴다.",
      ["시그모이드: 출력 0~1, 확률과 게이트에 사용",
       "시그모이드 약점: 양 끝 기울기 0, 기울기 소실",
       "ReLU: max(z, 0), 계산이 싸고 학습이 빠름",
       "ReLU 약점: 음수 쪽 기울기 0, dying ReLU",
       "대안 Leaky ReLU 는 음수에도 작은 기울기",
       "GELU: 부드러운 ReLU, 트랜스포머가 사용",
       "기본 지침: 안쪽은 ReLU/GELU, 확률에만 시그모이드"])

essay(U3, "N3 p.13-14", "cnn-vs-rnn",
      "텍스트 **CNN(합성곱 신경망)**이 무엇을 계산하는지 설명하고, 어떤 과제에 알맞고 어떤 과제에는 부족한지 **RNN(순환 신경망)**과 비교해 서술하시오.",
      "**CNN(합성곱 신경망)**은 폭 k 의 **합성곱 필터(Convolution Filter)**를 문장 위로 한 칸씩 미끄러뜨려 창마다 값 하나를 내고, 그 값들을 모은 **특징 맵(Feature Map)**에 **최대 풀링(Max-pooling)**을 걸어 가장 센 반응만 남긴다. 폭 3 필터는 학습된 trigram 탐지기이고, 폭 2에서 5 필터를 여럿 두면 학습된 **n-gram(엔그램)** 탐지기 묶음이 된다. 강점은 모든 창을 동시에 계산해 빠르고, 최대 풀링 덕분에 패턴이 어디에 있든 잡아내며, 감정이나 주제 **분류(Classification)**에 실제로 오래 쓰였다는 점이다. 한계는 창 안의 가까운 곳만 보아 먼 거리 순서와 관계를 잡지 못한다는 것이다. 그래서 순서가 중요한 과제에는 **은닉 상태(Hidden State)**로 읽은 것을 이어 가는 **RNN(순환 신경망)**이 맞다. 슬라이드 한 줄로는 CNN 은 병렬 n-gram 탐지기, RNN 은 기억을 가진 독자이다.",
      ["필터를 미끄러뜨려 창마다 값 하나",
       "값들을 모은 것이 특징 맵",
       "최대 풀링으로 가장 센 반응만 남김",
       "폭 3 필터 = 학습된 trigram 탐지기, 폭 2~5 묶음 = n-gram 탐지기 뱅크",
       "강점: 병렬로 빠름, 위치 무관, 분류에 강함",
       "한계: 로컬만 봄, 먼 거리 순서 못 잡음",
       "순서가 필요하면 RNN(기억을 가진 독자)"])

essay(U4, "N3 p.25-26", "shape-checking",
      "역전파 식에 **전치(Transpose)**가 붙는 이유를 설명하고, **모양(Shape)** 검사를 어떻게 오류 찾기에 쓰는지 예를 들어 서술하시오.",
      "슬라이드의 솔직한 답은 그래야 차원이 맞기 때문이고, 이것은 결함이 아니라 쓸모 있는 기능이다. 약속은 **파라미터(Parameter)**의 **기울기(Gradient)**가 그 파라미터와 같은 shape 을 가져야 한다는 것이다. W ← W - α∂J/∂W 처럼 칸마다 빼야 하므로 두 표의 크기가 같아야 하기 때문이다. 검사 방법은 기호마다 밑에 shape 을 적고 곱셈마다 가운데 숫자가 같은지 보는 것이다. 예를 들어 x 가 5칸, W 가 (3, 5), δ 가 3칸이면 δxᵀ 는 (3×1)(1×5) = (3, 5) 로 W 와 같아 통과이고, Wᵀδ 는 (5×3)(3×1) = (5, 1) 로 x 와 같아 통과이다. 반대로 xδᵀ 는 (5, 3) 이 되어 W 와 다르므로 틀린 식이고, Wδ 는 가운데 5 와 3 이 달라 곱할 수조차 없다. 이렇게 하면 코드를 돌리기 전에 오류를 잡을 수 있고, 코드에서는 print(tensor.shape) 한 줄이 가장 쓸모 있다.",
      ["전치는 차원을 맞추려고 붙음",
       "약속: 파라미터의 기울기는 파라미터와 같은 shape",
       "이유: W ← W - α∂J/∂W 를 원소별로 빼야 해서",
       "방법: 기호마다 shape 을 적고 곱셈의 가운데를 확인",
       "예: δxᵀ = (3,5) = W (통과), Wᵀδ = (5,) = x (통과)",
       "틀린 예: xδᵀ = (5,3) 은 W 와 다름",
       "틀린 예: Wδ 는 가운데가 안 맞아 곱셈 불가",
       "코드에서는 print(tensor.shape) 로 확인"])

essay(U5, "N3 p.31-32", "backprop-cost-memory",
      "**역전파(Backpropagation)**의 진짜 가치가 '정확함' 이 아니라 '값(비용)' 이라는 슬라이드의 주장을 설명하고, 그 대가로 치르는 것이 무엇인지 서술하시오.",
      "**역전파(Backpropagation)**는 **순전파(Forward Pass)**를 한 번 하고 **역방향 패스(Backward Pass)**를 한 번 하면 모든 **파라미터(Parameter)**의 **기울기(Gradient)**를 한꺼번에 얻는다. 역방향 한 번의 비용이 순전파 한 번과 비슷하므로, 기울기 전부를 사실상 한 번 값에 사는 셈이다. 비교하면 파라미터를 하나씩 흔들어 보는 방법은 파라미터가 100만 개일 때 순전파가 100만 번 필요하다. 이 효율 덕분에 수십억 개 파라미터 모델도 한 스텝을 돌릴 수 있고, 슬라이드는 이것이 딥러닝이 계산상 가능한 이유라고 말한다. 대신 속도를 메모리로 산다. 역방향에서 **ReLU(렐루)**의 부호나 곱셈 노드의 상대 입력값이 필요하므로 순전파 중간값을 모두 저장해야 하고, 파라미터마다 기울기도 함께 들고 있어야 한다. 교수님은 그래서 학습에는 추론의 최소 두 배 메모리가 필요하고, GPU 16GB 에 모델이 10GB 면 추론은 되어도 학습은 안 된다고 설명했다.",
      ["순전파 1회 + 역방향 1회로 모든 기울기",
       "역방향 비용이 순전파 1회와 비슷",
       "하나씩 흔들면 파라미터 수만큼 순전파 필요",
       "그래서 대규모 모델 학습이 가능해짐",
       "대가는 메모리: 순전파 중간값 저장",
       "이유: ReLU 부호, 곱셈 노드의 상대값이 필요",
       "파라미터마다 기울기도 저장",
       "학습은 추론보다 최소 2배 메모리"])

essay(U6, "N3 p.39, p.44-45", "ngram-walls-and-neural-lm",
      "n-gram **언어 모델(LM)**의 세 벽을 쓰고, 고정 윈도우 신경망 **언어 모델(LM)**이 그 가운데 무엇을 무너뜨리고 무엇을 남겼는지 서술하시오.",
      "세 벽은 첫째 **희소성 문제(Sparsity Problem)**로, 묶음을 본 적이 없으면 확률이 0이 되거나 조건을 걸 수조차 없다. 둘째 저장 문제로, 본 적 있는 모든 **n-gram(엔그램)**의 횟수를 들고 있어야 해서 **말뭉치(Corpus)** 크기와 n 에 따라 불어난다. 셋째 유사도 없음으로, opened their 와 opened her 가 횟수를 전혀 나누지 못한다. 여기에 n-1 단어 밖 문맥이 보이지 않는 한계가 더해진다. 고정 윈도우 신경망 **언어 모델(LM)**은 단어를 **임베딩(Embedding)**으로 바꿔 계산하므로 본 적 없는 조합에도 그럴듯한 확률을 주고 거대한 횟수 표도 필요 없어지며, 비슷한 단어가 서로의 학습 결과를 나눈다. 즉 희소성, 저장, 유사도 세 벽을 허문다. 그러나 창이 여전히 고정이라 그 밖의 문맥은 버려지고, 자리마다 W 의 다른 부분을 쓰기 때문에 같은 단어가 자리마다 따로 학습되는 낭비가 남는다. 이 둘을 풀려고 어떤 길이든 처리하고 자리마다 같은 처리를 공유하는 **RNN(순환 신경망)**으로 간다.",
      ["벽 1: 희소성(분자 0, 분모 0)",
       "벽 2: 저장(모든 n-gram 횟수 보관)",
       "벽 3: 유사도 없음(their 와 her 가 남남)",
       "추가 한계: n-1 단어 밖 문맥이 안 보임",
       "고정 윈도우 신경망 LM 은 임베딩으로 희소성, 저장, 유사도를 해결",
       "남은 문제 1: 창이 고정이라 밖의 문맥은 버려짐",
       "남은 문제 2: 자리마다 가중치가 따로라 공유가 없음",
       "해결 방향: 어떤 길이도 처리하고 가중치를 공유하는 RNN"])

essay(U7, "N3 p.42, N3L p.24-26", "perplexity-trap",
      "실습에서 글자 모델과 **서브워드(Subword)** 모델의 **퍼플렉서티(PPL)**를 그대로 비교하면 왜 잘못된 결론이 나오는지, 그리고 어떻게 고쳐 재는지 서술하시오.",
      "실습에서 같은 평가 글에 대해 글자 모델은 PPL 27.48, **서브워드(Subword)** 모델은 PPL 299.07 이 나왔다. 숫자만 보면 글자 모델이 크게 이긴 것처럼 보이지만, PPL 은 예측 단위 하나당 값이다. 글자 모델은 다음 글자 하나를 맞히는 쉬운 문제를 풀고, 서브워드 모델은 평균 1.9 글자에 해당하는 덩어리를 한 번에 맞히는 어려운 문제를 푼다. 서로 다른 자로 잰 숫자를 나란히 놓은 셈이라 비교가 성립하지 않는다. 고치는 방법은 두 값을 bits per character 로 환산해 '이 모델이 한국어 글자 하나를 적는 데 몇 비트가 필요한가' 라는 같은 질문에 답하게 만드는 것이다. 그러면 글자 모델 4.78, 서브워드 모델 4.30 으로 오히려 서브워드 모델이 더 좋았다. 그래서 논문에서 perplexity 숫자를 볼 때는 어떤 단위, 어떤 **토크나이저(Tokenizer)** 기준인지부터 물어야 한다.",
      ["글자 27.48 대 서브워드 299.07 (겉보기)",
       "PPL 은 예측 단위 하나당 값",
       "글자 하나 맞히기와 덩어리 맞히기는 난이도가 다름",
       "서브워드 한 번이 평균 약 1.9 글자",
       "고치는 법: bits per character 로 환산",
       "같은 질문에 답하게 만드는 것이 핵심",
       "결과는 4.78 대 4.30 으로 뒤집힘",
       "논문의 PPL 은 단위와 토크나이저를 먼저 확인"])

essay(U8, "N3 p.48, p.50", "rnn-vs-fixed-window",
      "고정 윈도우 신경망 **언어 모델(LM)**과 **RNN(순환 신경망)** **언어 모델(LM)**을 문맥 길이, 가중치, 모델 크기, 계산 방식, 실제 망각의 다섯 가지로 비교하고, **시간 역전파(BPTT)**가 무엇인지 덧붙이시오.",
      "문맥 길이는 고정 윈도우가 창에 들어가는 만큼으로 고정이고, **RNN(순환 신경망)**은 원리상 제한이 없다. 가중치는 고정 윈도우가 자리마다 W 의 다른 부분을 쓰지만, RNN 은 하나의 W_h 와 W_e 를 모든 시점이 공유한다. 모델 크기는 고정 윈도우가 창이 커질수록 함께 커지지만, RNN 은 문장 길이와 무관하게 일정하다. 계산은 고정 윈도우가 한 번에 병렬로 끝나는 반면, RNN 은 t 번째가 t-1 을 기다려야 하는 순차 계산이라 느리다. 실제 망각은 고정 윈도우가 설계상 창 밖을 버리는 것이고, RNN 은 **기울기(Gradient)**가 흐려져서 먼 과거를 잊는다. **시간 역전파(BPTT)**는 이 RNN 을 펼친 그래프에 보통의 **역전파(Backpropagation)**를 하되, W_h 가 모든 시점에 나타나므로 그 기울기를 모든 등장 자리에서 더해 주는 것이다. 2부의 '갈림길에서는 기울기를 더한다' 규칙 그대로여서 새로 만든 것은 없다.",
      ["문맥 길이: 고정 대 원리상 제한 없음",
       "가중치: 자리마다 따로 대 W_h, W_e 공유",
       "모델 크기: 창과 함께 커짐 대 길이와 무관",
       "계산: 병렬 한 번 대 순차(t 가 t-1 을 기다림)",
       "망각: 설계상 버림 대 기울기 감소로 흐려짐",
       "BPTT = 펼친 그래프에 보통 역전파",
       "W_h 가 모든 시점에 나타나 기울기를 합산",
       "2부의 분기 합 규칙 그대로"])

# =====================================================================
# CALC (모든 숫자는 아래에서 실제로 계산하고 assert 로 확인)
# =====================================================================
# --- h1: 두 모델의 교차 엔트로피 비교
pa = np.array([0.9, 0.8])
pb = np.array([0.5, 0.1])
la = -np.log(pa)
lb = -np.log(pb)
ja, jb = float(la.mean()), float(lb.mean())
assert round(float(la[0]), 2) == 0.11 and round(float(la[1]), 2) == 0.22
assert round(ja, 2) == 0.16 and round(jb, 2) == 1.50
calc(U1, "N3 p.7", "cross-entropy-two-models",
     "두 분류기가 같은 두 문제를 풀었다. 모델 A 는 정답 클래스에 0.9 와 0.8 을, 모델 B 는 0.5 와 0.1 을 주었다. **교차 엔트로피(Cross-Entropy)** J = (1/N) Σ -log P(정답) 으로 (1) 모델 A 첫 문제의 벌점, (2) 모델 A 둘째 문제의 벌점, (3) 모델 A 의 평균 벌점 J, (4) 모델 B 의 평균 벌점 J 를 구하시오. (자연로그)",
     "문제마다 -log(정답 확률) 을 구한 뒤 평균을 내요. 소수 둘째 자리까지.",
     [{"label": "모델 A 첫 문제 벌점", "ans": round(float(la[0]), 2), "tol": 0.01},
      {"label": "모델 A 둘째 문제 벌점", "ans": round(float(la[1]), 2), "tol": 0.01},
      {"label": "모델 A 의 평균 J", "ans": round(ja, 2), "tol": 0.01},
      {"label": "모델 B 의 평균 J", "ans": round(jb, 2), "tol": 0.01}],
     ["-log(0.9) ≈ 0.11",
      "-log(0.8) ≈ 0.22",
      "모델 A: (0.11 + 0.22) ÷ 2 ≈ 0.16",
      "-log(0.5) ≈ 0.69, -log(0.1) ≈ 2.30",
      "모델 B: (0.69 + 2.30) ÷ 2 ≈ 1.50",
      "정답 확률이 0.1 인 한 문제가 평균을 크게 끌어올려요"],
     "A 의 두 벌점 0.11 과 0.22, 평균 J ≈ 0.16, B 의 평균 J ≈ 1.50",
     "확률을 평균 내고 나서 로그를 씌우면 안 돼요. 문제마다 -log 를 먼저 구하고 그다음에 평균을 내요. 크게 틀린 한 문제가 평균을 확 올리는 것이 **교차 엔트로피(Cross-Entropy)**의 성질이에요.")

# --- h2: 선형층 합치기와 ReLU 를 넣었을 때
W1 = np.array([[1.0, 2.0], [0.0, 1.0]])
W2 = np.array([[1.0, 0.0], [1.0, 1.0]])
xh = np.array([1.0, 1.0])
two_layer = W2 @ (W1 @ xh)
merged = W2 @ W1
merged_out = merged @ xh
V1 = np.array([[1.0, -2.0], [0.0, 1.0]])
with_relu = W2 @ np.maximum(V1 @ xh, 0.0)
without_relu = (W2 @ V1) @ xh
assert list(two_layer) == [3.0, 4.0] and list(merged_out) == [3.0, 4.0]
assert merged.tolist() == [[1.0, 2.0], [1.0, 3.0]]
assert list(with_relu) == [0.0, 1.0] and list(without_relu) == [-1.0, 0.0]
calc(U2, "N3 p.11", "linear-collapse-check",
     "W1 = [[1, 2], [0, 1]], W2 = [[1, 0], [1, 1]], x = [1, 1] 이다. (1) 한 층씩 계산한 W2(W1x) 의 두 칸과 (2) 먼저 합친 W2W1 의 2행 2열 값을 구하시오. 다음으로 V1 = [[1, -2], [0, 1]] 로 바꾸고 사이에 **ReLU(렐루)**를 넣어 (3) W2 ReLU(V1x) 의 두 칸을, ReLU 를 빼고 (4) (W2V1)x 의 첫 칸을 구하시오.",
     "행렬 곱을 순서대로 하고, ReLU 는 음수를 0으로 바꿔요. 정수로 답해요.",
     [{"label": "W2(W1x) 첫 칸", "ans": float(two_layer[0]), "tol": 0.01},
      {"label": "W2(W1x) 둘째 칸", "ans": float(two_layer[1]), "tol": 0.01},
      {"label": "W2W1 의 2행 2열", "ans": float(merged[1][1]), "tol": 0.01},
      {"label": "W2 ReLU(V1x) 첫 칸", "ans": float(with_relu[0]), "tol": 0.01},
      {"label": "W2 ReLU(V1x) 둘째 칸", "ans": float(with_relu[1]), "tol": 0.01},
      {"label": "ReLU 없는 (W2V1)x 첫 칸", "ans": float(without_relu[0]), "tol": 0.01}],
     ["W1x = [1×1 + 2×1, 0×1 + 1×1] = [3, 1]",
      "W2(W1x) = [1×3 + 0×1, 1×3 + 1×1] = [3, 4]",
      "먼저 합치면 W2W1 = [[1, 2], [1, 3]] 이고 (W2W1)x = [3, 4] 로 같아요",
      "V1x = [1 - 2, 1] = [-1, 1], ReLU → [0, 1]",
      "W2 [0, 1] = [0, 1]",
      "ReLU 를 빼면 (W2V1)x = [-1, 0] 으로 답이 달라져요"],
     "W2(W1x) = [3, 4], W2W1 의 2행 2열 = 3, ReLU 넣으면 [0, 1], ReLU 빼면 첫 칸 -1",
     "f 가 없으면 두 층이 **행렬(Matrix)** 하나로 합쳐져 같은 답이 나와요. ReLU 를 끼우면 합쳐 쓴 값과 달라지므로 하나로 못 합쳐요. 이것이 **비선형성(Nonlinearity)**이 필요한 이유예요.")

# --- h3: CNN 필터 뱅크의 창 개수와 특징 벡터 길이
n_words = 10
widths = [2, 3, 4, 5]
n_per_width = 100
wins = {k: n_words - k + 1 for k in widths}
feat_dim = len(widths) * n_per_width
assert wins[2] == 9 and wins[3] == 8 and wins[5] == 6 and feat_dim == 400
calc(U3, "N3 p.13-14", "cnn-filter-bank",
     "10단어 문장에 폭 2, 3, 4, 5 짜리 **합성곱 필터(Convolution Filter)**를 폭마다 100개씩 둔다. 필터는 한 칸씩 미끄러지고, 필터마다 **최대 풀링(Max-pooling)**으로 숫자 하나를 남긴다. (1) 폭 2 필터의 창 개수, (2) 폭 3 필터의 창 개수, (3) 폭 5 필터의 창 개수, (4) 풀링 뒤 분류기에 들어가는 벡터의 길이를 구하시오.",
     "창 개수는 (단어 수 - 폭 + 1) 이고, 풀링 뒤 숫자는 필터 하나당 하나예요. 정수로 답해요.",
     [{"label": "폭 2 필터의 창 개수", "ans": wins[2], "tol": 0.01},
      {"label": "폭 3 필터의 창 개수", "ans": wins[3], "tol": 0.01},
      {"label": "폭 5 필터의 창 개수", "ans": wins[5], "tol": 0.01},
      {"label": "풀링 뒤 벡터 길이", "ans": feat_dim, "tol": 0.01}],
     ["창 개수 = 단어 수 - 폭 + 1",
      "폭 2: 10 - 2 + 1 = 9",
      "폭 3: 10 - 3 + 1 = 8",
      "폭 5: 10 - 5 + 1 = 6",
      "필터 총 개수 = 4가지 폭 × 100개 = 400개",
      "최대 풀링은 필터마다 숫자 하나를 남기므로 벡터 길이 400"],
     "창 개수 9, 8, 6 이고 풀링 뒤 벡터 길이 400",
     "풀링 뒤 벡터 길이는 창 개수와 상관이 없어요. 필터 개수만큼이에요. 창 개수를 단어 수(10)로 세면 틀려요.")

# --- h4: 수치 기울기로 기울기 검사
def fth(t):
    return t ** 3 + 2 * t

th, hstep = 2.0, 1e-4
analytic = 3 * th ** 2 + 2
fplus, fminus = fth(th + hstep), fth(th - hstep)
numeric = (fplus - fminus) / (2 * hstep)
assert analytic == 14.0
assert round(fplus, 4) == 12.0014 and round(fminus, 4) == 11.9986
assert abs(numeric - 14.0) < 1e-6
calc(U4, "N3 p.32, N3L p.4", "numeric-gradient-check",
     "f(θ) = θ³ + 2θ 를 θ = 2 에서 검사한다. 손 공식은 f'(θ) = 3θ² + 2 이고, **수치 기울기(Numeric Gradient)**는 (f(θ+h) - f(θ-h)) / 2h 로 h = 0.0001 을 쓴다. (1) 손 공식 값, (2) f(2.0001), (3) f(1.9999), (4) 수치 기울기를 구하시오.",
     "손 공식과 수치 기울기가 거의 같으면 **기울기 검사(Gradient Checking)** 통과예요. (2)와 (3)은 소수 넷째 자리까지, 나머지는 소수 둘째 자리까지.",
     [{"label": "손 공식 f'(2)", "ans": analytic, "tol": 0.01},
      {"label": "f(2.0001)", "ans": round(fplus, 4), "tol": 0.0005},
      {"label": "f(1.9999)", "ans": round(fminus, 4), "tol": 0.0005},
      {"label": "수치 기울기", "ans": 14.0, "tol": 0.01}],
     ["손 공식: f'(2) = 3 × 2² + 2 = 12 + 2 = 14",
      "f(2.0001) = 2.0001³ + 2 × 2.0001 ≈ 12.0014",
      "f(1.9999) = 1.9999³ + 2 × 1.9999 ≈ 11.9986",
      "차이 ≈ 0.0028, 2h = 0.0002",
      "수치 기울기 = 0.0028 ÷ 0.0002 ≈ 14.0",
      "손 공식 14 와 거의 같아요 → 검사 통과"],
     "손 공식 14, f(2.0001) ≈ 12.0014, f(1.9999) ≈ 11.9986, 수치 기울기 ≈ 14.0",
     "식의 h 는 아주 작은 수(0.0001)이지 **은닉층(Hidden Layer)** 값 h 가 아니에요. 분모를 h 가 아니라 2h 로 나누어야 해요. 수치 기울기는 느려서 학습이 아니라 검사에만 써요.")

# --- h5: 계산 그래프 노드 세 가지
a_, b_, c_ = 1.0, 3.0, 2.0
q_ = a_ + b_
r_ = max(b_, c_)
f_ = q_ * r_
df_dq, df_dr = r_, q_
df_da = df_dq * 1.0
df_db = df_dq * 1.0 + df_dr * (1.0 if b_ > c_ else 0.0)
df_dc = df_dr * (1.0 if c_ > b_ else 0.0)
assert q_ == 4.0 and r_ == 3.0 and f_ == 12.0
assert df_da == 3.0 and df_db == 7.0 and df_dc == 0.0
calc(U5, "N3 p.29-30", "backprop-node-graph",
     "f = (a + b) × max(b, c) 이고 a = 1, b = 3, c = 2 이다. **계산 그래프(Computation Graph)** 위에서 역전파하여 (1) f 의 값, (2) ∂f/∂a, (3) ∂f/∂b, (4) ∂f/∂c 를 구하시오. (+ 는 그대로 나눠 주고, max 는 이긴 쪽에만 보내고, × 는 상대 값을 곱한다.)",
     "순전파로 값을 먼저 구한 뒤 출력에서 1로 시작해 거꾸로 내려와요. b 가 두 군데 쓰인 점에 주의해요. 정수로 답해요.",
     [{"label": "f 의 값", "ans": f_, "tol": 0.01},
      {"label": "∂f/∂a", "ans": df_da, "tol": 0.01},
      {"label": "∂f/∂b", "ans": df_db, "tol": 0.01},
      {"label": "∂f/∂c", "ans": df_dc, "tol": 0.01}],
     ["순전파: q = a + b = 4, r = max(3, 2) = 3, f = 4 × 3 = 12",
      "출발 ∂f/∂f = 1. × 노드는 맞바꿔요: q 쪽 1 × 3 = 3, r 쪽 1 × 4 = 4",
      "+ 노드는 그대로 나눠 줘요: a 에 3, b 에 3",
      "max 노드는 이긴 쪽(b = 3 > c = 2)에만: b 에 4, c 에 0",
      "b 는 두 곳에 쓰였으니 합쳐요: 3 + 4 = 7",
      "확인: ∂f/∂b = max(b, c) + (a + b) = 3 + 4 = 7"],
     "f = 12, ∂f/∂a = 3, ∂f/∂b = 7, ∂f/∂c = 0",
     "× 노드는 자기 값이 아니라 상대 값을 곱해요. b 처럼 두 갈래로 쓰인 값은 두 기울기를 더해야 하고, 큰 쪽만 골라 쓰면 틀려요. max 에서 진 쪽 c 의 0 은 '출력에 영향이 없었다' 는 뜻이에요.")

# --- h6: shape 으로 식 검사하기
x_len, W_shape, d_len = 5, (3, 5), 3
dW_shape = (d_len, x_len)
dW_cells = dW_shape[0] * dW_shape[1]
dx_len = x_len
wrong_shape = (x_len, d_len)
assert dW_shape == W_shape and dW_cells == 15 and dx_len == 5 and wrong_shape == (5, 3)
calc(U5, "N3 p.25-26, N3L p.3", "shape-check-grad",
     "x 가 5칸, W 가 (3, 5), δ 가 3칸이다. (1) ∂s/∂W = δxᵀ 의 행 수, (2) 열 수, (3) 칸(원소) 개수, (4) ∂s/∂x = Wᵀδ 의 길이, (5) 식을 xδᵀ 로 잘못 썼을 때의 행 수를 구하시오.",
     "곱셈 규칙 (a×b)(b×c) = (a×c) 를 그대로 써요. 정수로 답해요.",
     [{"label": "∂s/∂W 의 행 수", "ans": dW_shape[0], "tol": 0.01},
      {"label": "∂s/∂W 의 열 수", "ans": dW_shape[1], "tol": 0.01},
      {"label": "∂s/∂W 의 칸 개수", "ans": dW_cells, "tol": 0.01},
      {"label": "∂s/∂x 의 길이", "ans": dx_len, "tol": 0.01},
      {"label": "xδᵀ 로 잘못 썼을 때의 행 수", "ans": wrong_shape[0], "tol": 0.01}],
     ["δxᵀ = (3×1)(1×5) = (3, 5) 이므로 행 3, 열 5",
      "칸 개수 = 3 × 5 = 15, W 의 칸 수와 같아요",
      "Wᵀδ = (5×3)(3×1) = (5, 1) 이므로 길이 5, x 와 같아요",
      "xδᵀ = (5×1)(1×3) = (5, 3) 이라 행이 5",
      "(5, 3) 은 W 의 (3, 5) 와 달라 틀린 식이에요"],
     "∂s/∂W 는 (3, 5) 로 15칸, ∂s/∂x 는 길이 5, 잘못 쓴 xδᵀ 는 행이 5",
     "**파라미터(Parameter)**의 **기울기(Gradient)** 칸 수는 파라미터 칸 수와 늘 같아요. 순서를 바꿔 쓰면 shape 이 뒤집혀 업데이트를 할 수 없고, Wδ 처럼 쓰면 가운데가 안 맞아 곱셈 자체가 안 돼요.")

# --- h7: trigram 생성 경로의 확률
chain = [0.31, 0.62, 0.57, 0.34, 0.41]
p1 = chain[0] * chain[1]
p2 = p1 * chain[2]
p3 = p2 * chain[3]
p4 = p3 * chain[4]
assert round(p1, 4) == 0.1922 and round(p2, 4) == 0.1096
assert round(p3, 4) == 0.0372 and round(p4, 4) == 0.0153
calc(U6, "N3 p.35, p.40", "trigram-generation-prob",
     "trigram **언어 모델(LM)**이 'today the' 뒤에 price(0.31), of(0.62), gold(0.57), is(0.34), shiny(0.41) 를 차례로 뽑았다. **확률의 연쇄 법칙(Chain Rule of Probability)**으로 (1) 앞 두 단어까지의 확률, (2) 앞 세 단어까지, (3) 앞 네 단어까지, (4) 다섯 단어 전체의 확률을 구하시오.",
     "확률을 차례로 곱해 나가요. 소수 넷째 자리까지 반올림해요.",
     [{"label": "price, of 까지", "ans": round(p1, 4), "tol": 0.0005},
      {"label": "gold 까지", "ans": round(p2, 4), "tol": 0.0005},
      {"label": "is 까지", "ans": round(p3, 4), "tol": 0.0005},
      {"label": "shiny 까지(전체)", "ans": round(p4, 4), "tol": 0.0005}],
     ["0.31 × 0.62 = 0.1922",
      "0.1922 × 0.57 ≈ 0.1096",
      "0.1096 × 0.34 ≈ 0.0372",
      "0.0372 × 0.41 ≈ 0.0153",
      "다섯 단어 경로의 확률은 약 1.5% 예요"],
     "0.1922 → 0.1096 → 0.0372 → 전체 약 0.0153",
     "글 전체 확률은 조건부 확률의 곱이라 단어가 늘수록 빠르게 작아져요. 그래서 실제로는 로그를 씌워 더해요. 이 다섯 번은 모두 1등 후보가 뽑힌 경우라, **샘플링(Sampling)**이 늘 1등을 고른다는 뜻은 아니에요.")

# --- h8: 손실과 퍼플렉서티 사이 오가기
loss_100, loss_600 = 4.183, 3.254
ppl_100, ppl_600 = math.exp(loss_100), math.exp(loss_600)
ppl_char = 27.48
bits_char = math.log2(ppl_char)
j_from_ppl = math.log(25.92)
assert round(ppl_100, 1) == 65.6 and round(ppl_600, 1) == 25.9
assert round(bits_char, 2) == 4.78 and round(j_from_ppl, 2) == 3.26
calc(U7, "N3 p.41, N3L p.12, p.25", "ppl-loss-bits",
     "**퍼플렉서티(PPL)** = exp(J) 이다. 실습 학습 로그에서 step 100 의 loss 는 4.183, step 600 의 loss 는 3.254 였다. (1) step 100 의 PPL, (2) step 600 의 PPL 을 구하시오. 또 (3) 평가에서 나온 글자 모델 PPL 27.48 을 bits per character 로 바꾸면(log2(PPL)) 얼마인지, (4) 학습 말뭉치 PPL 25.92 에 해당하는 J(log(PPL))가 얼마인지 구하시오. (자연로그와 밑이 2인 로그)",
     "PPL 과 J 는 exp 와 log 로 오갈 수 있어요. (1), (2)는 소수 첫째 자리까지, (3), (4)는 소수 둘째 자리까지.",
     [{"label": "step 100 의 PPL", "ans": round(ppl_100, 1), "tol": 0.1},
      {"label": "step 600 의 PPL", "ans": round(ppl_600, 1), "tol": 0.1},
      {"label": "PPL 27.48 의 bits/char", "ans": round(bits_char, 2), "tol": 0.01},
      {"label": "PPL 25.92 에 해당하는 J", "ans": round(j_from_ppl, 2), "tol": 0.01}],
     ["PPL = exp(J) 이므로 exp(4.183) ≈ 65.6",
      "exp(3.254) ≈ 25.9 (실습 로그의 ppl 25.9 와 같아요)",
      "bits per character = log2(PPL) = log2(27.48) ≈ 4.78",
      "거꾸로 J = log(PPL) = log(25.92) ≈ 3.26",
      "학습이 진행되면서 손실이 내려가면 PPL 도 함께 내려가요"],
     "step 100 PPL ≈ 65.6, step 600 PPL ≈ 25.9, bits/char ≈ 4.78, J ≈ 3.26",
     "exp 와 log 를 거꾸로 쓰지 않아요. bits 로 바꿀 때는 자연로그가 아니라 밑이 2인 로그를 써요. PPL 이 내려갔다고 해서 다른 **토크나이저(Tokenizer)**를 쓴 모델과 바로 비교할 수 있는 것은 아니에요.")

# --- h9: 실습 char-RNN 파라미터 개수
V, emb_dim, hid = 1945, 64, 256
p_emb = V * emb_dim
p_We = emb_dim * hid                      # bias=False
p_Wh = hid * hid + hid                    # bias=True
p_out = hid * V + V
total = p_emb + p_We + p_Wh + p_out
assert p_emb == 124480 and p_We == 16384 and p_Wh == 65792 and p_out == 499865
assert total == 706521
calc(U8, "N3L p.11", "rnn-param-count",
     "실습의 CharRNN 은 nn.Embedding(V, 64), W_e = Linear(64, 256, bias=False), W_h = Linear(256, 256, bias=True), out = Linear(256, V) 로 이루어지고 V = 1945 이다. (1) 임베딩 표의 **파라미터(Parameter)** 수, (2) W_e 의 수, (3) W_h 의 수(편향 항 포함), (4) 출력층의 수(편향 항 포함), (5) 전체 파라미터 수를 구하시오.",
     "Linear(in, out) 의 가중치는 in × out 개이고 bias=True 면 out 개가 더해져요. 정수로 답해요.",
     [{"label": "임베딩 파라미터 수", "ans": p_emb, "tol": 0.01},
      {"label": "W_e 파라미터 수", "ans": p_We, "tol": 0.01},
      {"label": "W_h 파라미터 수", "ans": p_Wh, "tol": 0.01},
      {"label": "출력층 파라미터 수", "ans": p_out, "tol": 0.01},
      {"label": "전체 파라미터 수", "ans": total, "tol": 0.01}],
     ["임베딩: 1945 × 64 = 124,480",
      "W_e: 64 × 256 = 16,384 (bias=False 라 더할 것이 없어요)",
      "W_h: 256 × 256 + 256 = 65,536 + 256 = 65,792",
      "출력층: 256 × 1945 + 1945 = 497,920 + 1,945 = 499,865",
      "합계: 124,480 + 16,384 + 65,792 + 499,865 = 706,521",
      "실습 출력의 706,521 parameters 와 같아요"],
     "임베딩 124,480, W_e 16,384, W_h 65,792, 출력층 499,865, 전체 706,521",
     "가장 큰 덩어리는 **어휘(Vocabulary)** 크기가 걸린 출력층이에요. W_e 에는 bias=False 라 편향 항을 더하면 안 돼요. 문장이 길어져도 이 숫자는 그대로인데, 같은 **가중치(Weight)**를 모든 시점에 다시 쓰기 때문이에요.")

# --- h10: 기울기 클리핑
g = np.array([6.0, 8.0])
tau = 5.0
gnorm = float(np.linalg.norm(g))
g_clipped = tau * g / gnorm if gnorm > tau else g
new_norm = float(np.linalg.norm(g_clipped))
assert gnorm == 10.0 and list(g_clipped) == [3.0, 4.0] and new_norm == 5.0
calc(U8, "N3 p.53, N3L p.12", "gradient-clipping",
     "**기울기 클리핑(Gradient Clipping)**의 식은 기울기 크기 ||g|| 가 한계 τ 보다 크면 g ← τ g / ||g|| 이다. g = [6, 8], τ = 5 일 때 (1) ||g||, (2) 클리핑 뒤 g 의 첫 칸, (3) 둘째 칸, (4) 클리핑 뒤의 크기를 구하시오. (||g|| 는 제곱해서 더한 뒤 제곱근)",
     "크기를 먼저 재고, 한계를 넘으면 같은 방향으로 크기만 τ 가 되게 줄여요. 정수로 답해요.",
     [{"label": "클리핑 전 크기", "ans": gnorm, "tol": 0.01},
      {"label": "클리핑 뒤 첫 칸", "ans": float(g_clipped[0]), "tol": 0.01},
      {"label": "클리핑 뒤 둘째 칸", "ans": float(g_clipped[1]), "tol": 0.01},
      {"label": "클리핑 뒤 크기", "ans": new_norm, "tol": 0.01}],
     ["||g|| = 제곱해서 더한 뒤 제곱근 = 6² + 8² = 100 의 제곱근 = 10",
      "10 > 5 이므로 클리핑을 해요",
      "g ← 5 × [6, 8] / 10 = [3, 4]",
      "새 크기 = 3² + 4² = 25 의 제곱근 = 5 = τ",
      "방향(6 대 8 의 비율 3 대 4)은 그대로예요"],
     "||g|| = 10, 클리핑 뒤 g = [3, 4], 크기는 정확히 5",
     "각 칸을 따로 5로 자르는 것이 아니라 벡터 전체 크기로 같은 비율만큼 줄여요. 그래야 방향이 보존돼요. 클리핑은 **기울기 폭발(Exploding Gradient)** 대책이지 **기울기 소실(Vanishing Gradient)** 대책이 아니에요.")

# =====================================================================
json.dump(bank, open(OUT, "w", encoding="utf-8"), ensure_ascii=False, indent=1)
from collections import Counter as _C
print(OUT, len(bank), dict(_C(q["type"] for q in bank)))
print("단원:", dict(_C(q["unit"] for q in bank)))
